version: 0.2

phases:
  pre_build:
    commands:
    - echo pre-build phase done
    
  build:
    commands:
    - echo build phase done
    
artifacts:
  files:
    - hello-world.py
    - imagedefinitions.json
  base-directory: